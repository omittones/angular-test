"use strict";

define(['libs/string', 'libs/log', 'libs/underscore'], function(string, log, _) {

    describe('it', function() {

        it('should succeed');

        it('should aslo succeed', function(){
            expect(1).toBe(1);
        });

    });

});